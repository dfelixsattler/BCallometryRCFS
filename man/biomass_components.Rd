% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/biomass_calculator.R
\name{biomass_components}
\alias{biomass_components}
\title{Compute aboveground tree biomass by component}
\usage{
biomass_components(species, dbh, height = NULL, paper_source = "Lambert2005")
}
\arguments{
\item{species}{character vector. Common species names (case-insensitive).}

\item{dbh}{numeric vector. Diameter at breast height (cm). Must be
the same length as \code{species}.}

\item{height}{numeric vector or \code{NULL}. Total tree height (m). If
supplied as a vector, trees where \code{height[i]} is not \code{NA}
use the DBH + height equations; trees where \code{height[i]} is
\code{NA} automatically fall back to the DBH-only equations. Pass
\code{NULL} (default) to use DBH-only equations for all trees.
Must be the same length as \code{species} if not \code{NULL}.}

\item{paper_source}{character. One of \code{"Lambert2005"} (default) or
\code{"Ung2008"}.}
}
\value{
A data frame with \code{length(dbh)} rows and five columns:
  \code{wood}, \code{bark}, \code{branches}, \code{foliage}, and
  \code{total} (sum of all four components). All values are dry biomass in
  kilograms.
}
\description{
Returns a data frame of dry biomass (kg) broken down into wood, bark,
branches, and foliage for each tree. Uses the same equations and argument
conventions as \code{\link{biomass_tree}}.
}
\examples{
biomass_components(
  species = c("douglas-fir", "western hemlock", "western redcedar"),
  dbh     = c(30, 25, 40),
  height  = c(28, 22, 35)
)
}
\seealso{
\code{\link{biomass_tree}}, \code{\link{biomass_coefs}}
}
