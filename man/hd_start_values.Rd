% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hd_model_fitting.R
\name{hd_start_values}
\alias{hd_start_values}
\title{Compute starting values for height-diameter model fitting}
\usage{
hd_start_values(dbh, height, model)
}
\arguments{
\item{dbh}{numeric vector. Diameter at breast height (cm).}

\item{height}{numeric vector. Total tree height (m). Must be the same length
as \code{dbh}.}

\item{model}{character. One of \code{"naslund"}, \code{"curtis"},
\code{"logistic"}, \code{"korf"}, \code{"weibull"}, \code{"richards"}.
Case-insensitive.}
}
\value{
A named list of starting values, e.g. \code{list(a = ..., b = ...)}
  for two-parameter models or \code{list(a = ..., b = ..., c = ...)} for
  three-parameter models.
}
\description{
Returns a named list of reasonable starting parameter values for the
specified height-diameter model form. Useful when calling \code{nls} or
\code{nlme} directly. Two-parameter models (\code{naslund}, \code{curtis})
use a linearisation strategy; three-parameter models use heuristics based
on the observed height range.
}
\examples{
set.seed(1)
dbh <- runif(100, 5, 60)
ht  <- ht_korf(dbh, a = 38, b = 2.5, c = 0.4) + rnorm(100, 0, 1)
hd_start_values(dbh, ht, model = "korf")
}
\seealso{
\code{\link{fit_hd_model}}
}
